export default function SpendElonMuskMoneyLayout({
    children,
  }: {
    children: React.ReactNode;
  }) {
    return (
      <div className="w-full h-full m-0 p-0">
        {children}
      </div>
    );
  }